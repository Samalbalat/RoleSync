package com.rolesync.rolesync.model;

/**
 * The ProfileType enum defines the different types of profiles that can be associated with a user in the RoleSync application.
 * The available profile types are ADMIN, USER, TABLETOP, and WRITTEN.
 */
public enum ProfileType {
    ADMIN,
    USER,
    TABLETOP,
    WRITTEN
}
